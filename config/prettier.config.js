// @ts-check

/**
 * @type {import('./types').PrettierConfig}
 */
const config = {
  singleQuote: true,
  arrowParens: 'always',
  semi: true,
  bracketSpacing: true,
  trailingComma: 'es5',
  printWidth: 80,
};

module.exports = config;
